# 이 파일을 복사해서 .env 로 만들고 값을 채우세요 (cp .env.example .env)
# .env 는 비밀값이라 깃에 올라가지 않습니다.

# 로그인 세션 서명키 — 아무도 모르는 긴 무작위 문자열
SECRET_KEY=change-me-to-a-long-random-string

# 마스터(관리자)는 '제일 먼저 가입한 사람'이 자동으로 됩니다. 별도 설정 불필요.
