# ETF 추천·분석 웹앱 — 작업 노트

개인용(부부 사용) 미국 ETF 추천·분석 웹앱. 위험 성향과 목표를 고르면 대표 미국 ETF를 점수화해 보여준다. 교육·참고용이며 투자 권유가 아니다.

최종 업데이트: 2026-06-22

---

## 1. 구성

| 구분 | 내용 |
|------|------|
| 백엔드 | Python + FastAPI, 단일 파일 `main.py` |
| 데이터 | yfinance (실패 시 종목별 폴백값 + `source` 표시) |
| 프론트 | `static/index.html` 단일 파일 + Chart.js |
| 포트 | 8000 |

```
ETF/
├─ main.py              # FastAPI 서버 + 추천 로직
└─ static/
   └─ index.html        # 화면 전체 (HTML+CSS+JS 단일 파일)
```

---

## 2. 만든 기능

### 입력 (3개 탭으로 구성 — 페이지 길이 단축)
- **추천 조건** 탭: 위험 성향(안정형/중립형/공격형), 투자 목표(성장/배당/분산)
- **종목** 탭: 테마 빠른선택(전체/성장/배당/채권/분산/섹터), 분석할 종목 31개 체크박스(처음엔 전체 선택)
- **필터** 탭: 배당 여부(전체/배당주만/무배당만), 최소 배당수익률(0/2/3/4%+), 표시 개수(Top5/Top10/전체)
- 탭 전환은 보여주는 항목만 바꾸고 설정은 유지된다.

### 동작
- "추천 받기"를 눌러야 결과가 뜬다 (페이지 로드 시 자동으로 무거운 계산을 돌리지 않음).
- 첫 실행 이후에는 어느 탭이든 값을 바꾸면 결과가 실시간으로 갱신된다.
- 종목별로 "왜 추천인지" 한국어 설명과 Yahoo Finance 새 탭 링크 제공.
- 결과는 점수순 정렬 + 막대 차트. 상단에 적용 필터·환율·표시 개수 요약.

### 필터 (점수에는 영향 없음 — 표시 범위만 결정)
- 배당 여부(dividends): 실제 배당수익률 기준 yes/no.
- 최소 배당수익률(min_yield): X% 이상만. "무배당만" 선택 시 자동 비활성(모순 방지).
- 표시 개수(top): 점수 상위 N개. matched(조건 통과 총수)도 함께 표기.

### 환율 (USD/KRW)
- yfinance "KRW=X"로 현재 환율 조회(실패 시 폴백 1385, 변동률 미상).
- 종목별 현재가를 달러+원화로 표시.
- 원화 환산 1년 수익률 = (1+달러수익률)×(1+환율 1년 변동률)−1. 실시간 가격·환율이 둘 다 있을 때만 계산(폴백이면 '—').
- 점수는 종전대로 달러 기준이며, 환율은 점수에 영향을 주지 않는다(참고용).

### 성능
- 종목 지표 + 환율을 ThreadPoolExecutor(최대 16+1)로 동시 수집 → 31종목 추천 대기 시간 대폭 단축.

### 추천 대상 풀 (31개)
- 성장: VOO, VTI, QQQ, VUG, SCHG
- 배당: SCHD, VYM, DGRO, HDV, VIG
- 채권: BND, AGG, TLT, SHY, BNDX
- 분산: VT, VEA, VWO, VNQ, GLD
- 섹터: XLK(기술), XLF(금융), XLV(헬스케어), XLE(에너지), XLY(임의소비재), XLP(필수소비재), XLI(산업재), XLU(유틸리티), XLB(소재), XLRE(리츠), XLC(커뮤니케이션서비스) — 미국 11개 GICS 섹터 SPDR Select Sector ETF

> 섹터 11종의 yield/ret1y 폴백값은 stockanalysis.com 조회값(2026-06-19 기준). vol 폴백값은 직접 제공되지 않아 베타×시장변동성(VOO 기준 ≈17%)을 기본으로, 베타가 못 잡아내는 섹터 특성(예: 에너지의 원자재 변동성, 유틸리티/필수소비재의 낮은 변동성, 기술/임의소비재의 높은 변동성)을 반영해 추정. yfinance가 살아있으면 이 폴백 대신 실데이터를 쓴다.

---

## 3. 점수 계산식 (화면 설명과 1:1 일치)

종목별 지표(yfinance로 계산, 실패 시 폴백):
- 1년 수익률(%) — 최근 1년 가격 변화
- 변동성(%) — 일간수익률 표준편차 × √252 (연 환산)
- 배당수익률(%) — 최근 12개월 배당 합계 ÷ 현재가

세부점수(0~100):
- 수익성 R = (1년수익률 + 10) / 50 × 100
- 배당   D = 배당수익률 / 5 × 100
- 안정성 S = (35 − 변동성) / 35 × 100

목표별 기본 가중치:

| 목표 | 수익성 | 배당 | 안정성 |
|------|-------|------|-------|
| 성장 | 0.60 | 0.10 | 0.30 |
| 배당 | 0.20 | 0.60 | 0.20 |
| 분산 | 0.34 | 0.33 | 0.33 |

위험 성향 보정 (shift = 0.20):
- 안정형: 안정성 +0.20 (수익성·배당에서 0.10씩 차감)
- 중립형: 변화 없음
- 공격형: 수익성 +0.20, 안정성 −0.20
- 보정 후 음수는 0으로 자르고 합이 1이 되도록 재정규화

최종점수 = 수익성×wR + 배당×wD + 안정성×wS (0~100)

> 가정: 점수는 **달러 기준** 수익률·변동성·배당으로 계산한다. 세금·매매수수료는 반영하지 않는다.
> 환율(USD/KRW)과 원화 환산 수익률은 참고로 따로 표시하며 점수에는 영향을 주지 않는다(환율도 늘 변동). 모든 수치는 교육·참고용.

---

## 4. 디자인 (Apple 톤)

- 단색 액션블루 `#0066cc` 하나만 사용 (두 번째 강조색·그라데이션 없음)
- Inter 폰트, 넉넉한 여백, 그림자 최소, 알약형 버튼
- 반응형: PC는 좌우 2단(입력 | 결과), 핸드폰은 위아래 세로 1단 자동 전환
- 모바일 보강: 여백·글자 크기 축소, 버튼·체크박스 터치 영역 확대, "추천 받기" 후 결과로 자동 스크롤, 노치 대응(viewport-fit)

---

## 5. 안정성 처리

- yfinance가 실패하거나 네트워크가 막혀도 앱이 죽지 않고 종목별 폴백값을 쓴다. 화면 우측 상단 배지로 항상 구분: `실시간 (yfinance)` / `기본값 (fallback)` / `일부 기본값 (mixed)`.
- yfinance 호출에 timeout(5초)을 둬 느릴 때 빠르게 폴백한다.
- 프론트는 서버 연결 실패나 `file://` 직접 열기를 감지해 원인과 해결법을 한국어 배너로 안내하고, 그래도 종목 목록은 비상용 내장 목록으로 항상 표시한다.
- Chart.js 로드 실패 시에도 카드·수치는 정상 표시된다.

---

## 6. 고친 문제

증상: 종목 목록이 안 나오고 "추천 받기"가 작동 안 함.

원인: 프론트 JS에 에러 처리가 없어, 파일을 직접 열거나(`file://`) 서버 미실행 등 한 가지라도 실패하면 목록을 못 불러오고 안내 없이 멈췄다. 체크박스가 비면 선택 종목이 없어 추천도 동작하지 않았다. (서버 자체는 정상 확인됨.)

조치: 위 5번의 안정성 처리를 추가. 비상용 목록 내장, 연결 실패·`file://` 감지 배너, 호출 실패 메시지, Chart.js 방어 코드.

---

## 7. 실행 방법

```bash
pip install fastapi uvicorn yfinance
python main.py          # 또는: uvicorn main:app --port 8000
```

브라우저에서 `http://127.0.0.1:8000` 접속. (파일을 직접 더블클릭하면 작동하지 않음 — 반드시 서버를 켜고 주소로 접속.)

### 핸드폰에서 보기 (같은 WiFi)
1. PC에서 서버 실행 (`0.0.0.0:8000`으로 열려 폰 접속 가능).
2. PC 내부 IP 확인: 명령창에서 `ipconfig` → IPv4 주소 (예: 192.168.0.10).
3. 폰 브라우저에서 `http://192.168.0.10:8000` 접속.

### 집 밖에서도 접속 (인터넷 배포 — Render 무료)
PC를 꺼도 폰에서 언제든 접속하려면 클라우드에 올린다. 저장소에 배포 설정을 포함해 둠:
- `Procfile`, `render.yaml` : 배포 명령(호스트가 주입하는 `$PORT` 사용).
- `main.py` 는 `PORT` 환경변수를 읽어 어떤 호스트에서도 동작.

배포 순서:
1. 이 저장소를 GitHub에 푸시(이미 되어 있음).
2. https://render.com 가입 → New → Web Service → 이 저장소 연결.
3. `render.yaml` 을 자동 인식해 빌드·실행 → `https://etf-recommender-XXXX.onrender.com` 주소가 생김.
4. 그 주소를 폰/PC 어디서나 열면 됨. 폰 홈화면에 추가하면 앱처럼 쓸 수 있음.

참고: Render 무료 플랜은 일정 시간 미사용 시 잠들었다가 첫 접속에서 ~30초 깨어남(이후엔 빠름).

### 회원가입 승인제 + 관리자(마스터)
- `ADMIN_EMAIL` 환경변수로 지정한 이메일이 **마스터(관리자)**. 그 이메일로 가입하면 자동 승인·관리자가 됨.
- 다른 사람이 가입하면 **승인 대기** 상태 → 앱 사용 차단("승인 대기중" 화면, 15초마다 자동 확인).
- 관리자는 헤더의 **"승인 대기 N 🔔"** 링크로 승인 패널을 열어 **승인/거절(삭제)**. (앱 안에서 확인하는 방식)
- 미승인자는 로그인은 되지만 앱 API 호출 시 403. 관리자 외 관리 API 호출도 403. 관리자 자기 계정 삭제는 차단.
- API: `/api/admin/users`, `/api/admin/approve`, `/api/admin/reject`.

### 회원가입·로그인·즐겨찾기 (계정 시스템)
- 기존 단일 공유 비밀번호(`APP_PASSWORD`)는 **제거**되고, **각자 회원가입** 방식으로 바뀜.
- 데이터는 **SQLite**(`DATA_DIR/app.db`)에 저장. 표준 라이브러리(sqlite3/hashlib/hmac)만 사용.
  - users(이메일·비번해시), favorites(사용자별 즐겨찾기 티커).
  - 비밀번호는 PBKDF2-HMAC-SHA256(솔트+20만회) 해시로 저장(원문 저장 안 함).
  - 로그인 상태는 서명 쿠키(user_id+HMAC, 서버 비밀키 `SECRET_KEY`). 30일 유지.
- API: `/api/register`, `/api/login`, `/api/logout`, `/api/me`,
  `/api/favorites`(GET/POST/DELETE).
- 화면: `/login`에서 로그인↔회원가입 토글. 결과 카드의 ★로 즐겨찾기 추가/삭제,
  종목 탭의 "⭐ 즐겨찾기만 보기"로 내 즐겨찾기만 분석.
- `DATA_DIR` 환경변수로 저장 위치 지정(미설정 시 `./data`). `SECRET_KEY` 미설정 시
  `DATA_DIR/secret.key`에 자동 생성·보관(재시작해도 로그인 유지).

### NAS에서 실행 (Docker — 데이터가 NAS에 저장됨)
회원·즐겨찾기 정보를 내 NAS에 저장하려면 앱을 NAS에서 돌린다. 저장소에 포함:
- `Dockerfile`, `docker-compose.yml`, `.dockerignore`.

순서(Synology Container Manager / QNAP Container Station 또는 SSH):
1. 이 저장소를 NAS에 내려받기(git clone 또는 압축 복사).
2. `docker-compose.yml`에서 `SECRET_KEY`(긴 무작위 문자열)와 `ADMIN_EMAIL`(내 이메일=마스터)을 설정.
3. 폴더에서 `docker compose up -d --build` 실행.
4. 폰/PC에서 `http://<NAS_IP>:8000` 접속 → **먼저 ADMIN_EMAIL로 가입**(자동 관리자).
   이후 다른 사람이 가입하면 관리자 화면에서 승인해야 사용 가능.
- DB·세션키는 `./data`(컨테이너 `/data`)에 저장되어 컨테이너를 지워도 유지됨.
- 집 밖에서도 쓰려면 NAS의 리버스 프록시/DDNS로 HTTPS 노출(각 NAS 문서 참고).
  HTTPS면 세션 쿠키가 Secure로 발급됨.

### (구) 인터넷 배포 — Render 무료
> 참고용. Render에 두면 집 NAS에 저장하기 어려우므로, 계정·즐겨찾기를 쓰려면 위 NAS 실행을 권장.
> Render에 그대로 둘 경우 DB는 Render 디스크에 저장되며 무료 플랜은 영구 디스크가 없어 재배포 시 초기화될 수 있음.

---

## 8. 검증 현황

- 백엔드: 실제 HTTP로 `/`(200), `/api/etfs`(31개, 5개 카테고리 — 성장/배당/채권/분산 5개씩 + 섹터 11개) 정상.
- 섹터 추가 검증: `/api/recommend`에 섹터 티커만 넘겨 성장+공격형으로 호출 → XLK(1년수익률 59.6%)가 1위로 정렬, 점수·근거 문구 모두 기존 공식 그대로 계산됨을 확인. yfinance가 막힌 환경에서도 전 종목 폴백으로 전환되며 앱이 죽지 않음을 재확인(`source: fallback`).
- 점수: 9개(성향×목표) 가중치 합 = 1.0 확인. 배당+안정형 → 채권·배당 ETF 상위, 성장+공격형 → 성장 ETF 상위로 의도대로 정렬.
- 프론트: 화면 JS 전체 문법 검사 통과, 파일 온전함 확인. `POOL_FALLBACK`(비상용 내장 목록)에도 섹터 11종 반영.
- 미검증: 이 작업 환경은 브라우저 설치가 막혀 실제 브라우저 클릭까지의 검증은 못 함 → PC에서 한 번 확인 필요.

---

## 9. 다음에 할 수 있는 것

- (완료) 실거래 추적 — 2026-06-22, 매수기록(날짜·티커·수량·단가) localStorage, 현재가 대비 평가손익(USD·원화·%), 실제배당 vs 예상배당 정확도
- (완료) 설정 저장·공유 — 2026-06-22, 설정 localStorage 저장(최대 5)·URL 공유 링크 복원
- (완료) 장점·단점 + 점수근거 펼치기 — 2026-06-22, 실제 지표에서 장단점 1줄씩
- (완료) 폰 모양 한 칸 레이아웃 + 즉시 결과 — 2026-06-22, PC도 폰처럼, 결과 위/종목 아래, 기본 배당 자동표시
- (완료) 로그인 아이디 방식 + 개방 모드 — 2026-06-22, ADMIN_USER 미설정 시 로그인 없이 사용(와이프 웹 접속)
- (완료) 환율 기본값 1500 + USDKRW 폴백 — 2026-06-22
- 배당 위주로 점수 단순화
- (완료) 회원가입·로그인·즐겨찾기 — 2026-06-22, SQLite 저장, NAS Docker 실행 지원
- (완료) 성향·목표 항상 노출 — 2026-06-22, 탭 위 고정 영역으로 이동(종목/필터 2탭)
- (완료) 홈화면 아이콘(PWA) — 2026-06-22, 매니페스트 + 아이콘(static/pwa/) + 메타태그.
  iOS는 공유→홈 화면에 추가, Android는 메뉴→홈 화면에 추가. standalone 전체화면 실행.
  아이콘은 Pillow로 생성(런타임 의존성 아님). 비밀번호 ON이어도 매니페스트·아이콘은 공개.
- (완료) 입력 탭 분리 — 2026-06-22, 추천 조건/종목/필터 3탭으로 페이지 단축
- (완료) 환율 반영 — 2026-06-22, USD/KRW 환율·원화 가격·원화 환산 수익률 표시(점수는 달러 기준 유지)
- (완료) 필터 — 2026-06-22, 테마·배당 여부·최소 배당수익률·Top N
- (완료) 성능 — 2026-06-22, yfinance 지표·환율 병렬 수집
- (완료) 접속 비밀번호 — 2026-06-22, `APP_PASSWORD` 환경변수로 로그인 보호(HMAC 쿠키, 표준 라이브러리만)
- (완료) 외부 접속 준비 — 2026-06-22, `PORT` 환경변수 대응 + `Procfile`/`render.yaml` 추가(클라우드 배포 가능)
- (완료) 캐시 개선 — 2026-06-22, 폴백 미캐시(고착 방지) + 실시간 1시간 TTL
- (완료) ETF 추가/카테고리 확장 — 2026-06-22, 섹터 11종 추가로 20개 → 31개
